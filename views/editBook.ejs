<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book - College Library</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <h1>📚 College Library</h1>
            <p class="subtitle">Edit Book Information</p>
        </div>

        <!-- Edit Book Form -->
        <div class="form-container">
            <h1>✏️ Edit Book: <%= book.name %></h1>
            <form method="POST" action="/edit/<%= book._id %>">
                <div class="form-group">
                    <label for="name">📖 Book Name</label>
                    <input type="text" id="name" name="name" value="<%= book.name %>" placeholder="Enter the book title..." required>
                </div>
                
                <div class="form-group">
                    <label for="author">✍️ Author Name</label>
                    <input type="text" id="author" name="author" value="<%= book.author %>" placeholder="Enter the author's name..." required>
                </div>
                
                <div class="form-group">
                    <label for="floor">🏢 Floor</label>
                    <select id="floor" name="floor" required>
                        <option value="">Select Floor</option>
                        <option value="G" <%= book.floor === 'G' ? 'selected' : '' %>>G</option>
                        <option value="1" <%= book.floor === '1' ? 'selected' : '' %>>1</option>
                        <option value="2" <%= book.floor === '2' ? 'selected' : '' %>>2</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <label for="rack">📋 Rack Number</label>
                    <input type="number" id="rack" name="rack" value="<%= book.rack %>" placeholder="Enter rack number..." required min="1">
                </div>
                
                <button type="submit" class="btn">💾 Update Book</button>
            </form>
        </div>

        <!-- Navigation -->
        <div class="nav">
            <a href="/" class="btn btn-secondary back-btn">🔙 Back to Home</a>
            <a href="/books" class="btn btn-secondary">🔍 Browse All Books</a>
            <form method="POST" action="/logout" style="display:inline;">
                <button type="submit" class="btn btn-secondary">🚪 Logout</button>
            </form>
        </div>
    </div>
</body>
</html>
